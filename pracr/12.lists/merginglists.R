# Create two lists.
list1 <- list(1,2,3)
list2 <- list("Sun","Mon","Tue")
# Merge the two lists.
merged.list <- c(list1,list2)
# Print the merged list.
print(merged.list)

print("Doing what I want to do: create a list that has two lists inside.")

outer.list<-list(list1,list2)
print(outer.list)

