# Create the data frame.

BMI <- data.frame(
                  gender = c("male","male","Female"),
                  height = c(152,171.5,165),
                  weight = c(81,93,78),
                  age = c(42,38,26)
                  )

print(BMI)
